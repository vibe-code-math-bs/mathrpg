/* ===== LOG.JS =====
   Placeholder — built in Phase B (Log modal: unit type -> skill -> source -> note ->
   save, wired to the §4 write fan-out + reward beat). openLogModal() not yet defined,
   so core.js falls back to navigating Today when the Log button is tapped. */
